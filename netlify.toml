[build]
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

# Configuration pour Next.js API routes
# IMPORTANT: Même avec le plan Pro, le timeout par défaut est de 26 secondes
# Pour augmenter au-delà, il faut contacter le support Netlify
# Alternative: Utiliser Background Functions pour les opérations longues
