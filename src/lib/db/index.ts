// Database operations exports
export { productDb } from './products';
export { analysisDb } from './analyses';

// Re-export supabase client (client-safe, uses NEXT_PUBLIC_ variables only)
export { supabase } from '../supabase';













